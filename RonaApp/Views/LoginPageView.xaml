<Page x:Class="RonaApp.Views.LoginPageView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:RonaApp.Views"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="LoginPageView">

    <Grid>
        
        <!-- Ejer3 -->
        <!-- Esta es la vista del login -->
        <!-- Creamos el stackpanel que contiene los textboxs bindeados que contienen la info del user-->

        <StackPanel Margin="16">
            <TextBlock Text="User"/>
            <TextBox Width="200" HorizontalAlignment="Left" Text="{Binding Username}"/>
            <TextBlock Text="Password"/>
            <TextBox Width="200" HorizontalAlignment="Left" Text="{Binding Pass}"/>


            <Button Margin="0,16,0,0" Width="100" Height="50" HorizontalAlignment="Left" Command="{Binding LoginCommand}">Login</Button>
        </StackPanel>

    </Grid>
</Page>
