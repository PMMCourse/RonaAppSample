<views:BaseView x:Class="RonaApp.Views.RegisterPatientView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RonaApp.Views"
        xmlns:views="clr-namespace:RonaApp.Views.Base"
        mc:Ignorable="d"
        Title="RegisterPatientView" Height="250" Width="250">
    <StackPanel HorizontalAlignment="Center" Margin="16">
        <!--Parte del Ejercicio 1 para completar el funcionamiento de la aplicación - hacer binding de los textbox hacia el viewmodel para conectar las variables-->
        <TextBlock Text="Name"/>
        <TextBox Width="200" HorizontalAlignment="Left" Text="{Binding Name}"/>
        <TextBlock Text="Surname"/>
        <TextBox Width="200" HorizontalAlignment="Left" Text="{Binding Surname}"/>
        <TextBlock Text="Virus affected"/>
        <ComboBox ItemsSource="{Binding AllVirus}">
            <ComboBoxItem>
                <TextBlock Text="{Binding Name}"/>
            </ComboBoxItem>
        </ComboBox>

        <Button Margin="0,16,0,0" Command="{Binding RegisterPatientCommand}">Register patient</Button>
    </StackPanel>
</views:BaseView>
